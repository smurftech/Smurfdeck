# SmurfDeck brand assets

These SVG sources consume the Smurftech V1 palette and product-icon conventions from `smurftech/brand-devkit` PR #2. SmurfDeck uses an independent control-deck mark rather than the parent-brand ST monogram; platform-specific raster exports should be generated at packaging time.
